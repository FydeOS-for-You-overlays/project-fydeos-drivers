diff --git a/Makefile b/Makefile
index 482e074..16c80e7 100644
--- a/Makefile
+++ b/Makefile
@@ -28,6 +28,9 @@ endif
 ifdef DRV_ROCKCHIP
 	CFLAGS += $(shell $(PKG_CONFIG) --cflags libdrm_rockchip)
 endif
+ifdef DEV_VC4
+	CFLAGS += $(shell $(PKG_CONFIG) --cflags libdrm_vc4)
+endif
 
 CPPFLAGS += $(PC_CFLAGS)
 LDLIBS += $(PC_LIBS)
